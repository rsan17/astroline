# 🚀 Інструкція для розгортання

## Проблема
Vercel не може автоматично створити проект з назвою папки "astroline 2" (через пробіл).

## Рішення

### Варіант 1: Через PowerShell (Рекомендовано)

1. **Відкрийте PowerShell в папці проекту:**
   ```powershell
   cd "C:\Users\rober\Desktop\astro\astroline 2"
   ```

2. **Додайте Node.js до PATH:**
   ```powershell
   $env:PATH += ";C:\Program Files\nodejs;$env:APPDATA\npm"
   ```

3. **Запустіть розгортання:**
   ```powershell
   vercel
   ```

4. **Відповіді на питання:**
   - **"Set up and deploy?"** → Натисніть `Y` (так)
   - **"Which scope?"** → Натисніть `Enter` (виберіть ваш scope)
   - **"Link to existing project?"** → Натисніть `N` (ні, створити новий)
   - **"What's your project's name?"** → Введіть: `astroline` (БЕЗ пробілів!)
   - **"In which directory is your code located?"** → Натисніть `Enter` (поточна папка `.`)
   - **"Want to override the settings?"** → Натисніть `N` (ні)

5. **Чекайте завершення розгортання** (2-5 хвилин)

6. **Після розгортання ви побачите URL**, наприклад:
   ```
   ✅ Production: https://astroline-xxxxx.vercel.app
   ```

### Варіант 2: Через Vercel Dashboard (Простіше)

1. **Відкрийте:** https://vercel.com/dashboard

2. **Натисніть:** "Add New" → "Project"

3. **Якщо немає репозиторію на GitHub:**
   - Натисніть "Browse" або перетягніть папку проекту
   - АБО спочатку завантажте на GitHub

4. **Налаштування проекту:**
   - **Framework Preset:** Next.js (автоматично)
   - **Root Directory:** `.` або залиште порожнім (якщо вже в папці проекту)
   - **Project Name:** `astroline` (БЕЗ пробілів!)
   - **Build Command:** `npm run build` (за замовчуванням)
   - **Output Directory:** `.next` (за замовчуванням)

5. **Натисніть:** "Deploy"

6. **Чекайте завершення** та скопіюйте URL

## Після розгортання

1. **Додайте змінні оточення:**
   - Відкрийте проект в Vercel Dashboard
   - Settings → Environment Variables
   - Додайте `NEXT_PUBLIC_APP_URL` = ваш URL

2. **Відкрийте сайт:**
   - Скопіюйте URL з Vercel Dashboard
   - Відкрийте в браузері

## Якщо все ще не працює

Перевірте:
- ✅ Ви авторизовані: `vercel whoami`
- ✅ Ви в правильній папці (де є `package.json`)
- ✅ Назва проекту БЕЗ пробілів: `astroline` (не `astroline 2`)
