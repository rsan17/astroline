# 📧 Resend Setup Guide - Як налаштувати email

Детальний гайд про те, як отримати Resend API key та як він працює в проєкті.

---

## 🎯 Що таке Resend?

**Resend** - це сервіс для відправки email. Він дозволяє вашому проєкту надсилати красиві HTML email з астрологічними звітами користувачам.

### Як це працює в Astroline:

1. Користувач проходить квіз
2. Генерується астрологічний звіт
3. Система автоматично надсилає email з посиланням на звіт
4. Користувач отримує красивий email з інформацією про свій знак зодіаку

---

## 🔑 Крок 1: Отримати Resend API Key

### Покрокова інструкція:

#### 1. Зареєструйтесь на Resend

1. Перейдіть на **https://resend.com**
2. Натисніть **"Sign Up"** (Зареєструватись)
3. Можна зареєструватись через:
   - Email
   - Google акаунт
   - GitHub акаунт

#### 2. Підтвердіть email

- Перевірте пошту та підтвердіть акаунт
- Після підтвердження ви автоматично потрапите в Dashboard

#### 3. Створіть API Key

1. В Dashboard перейдіть в **"API Keys"** (в лівому меню)
   - Або перейдіть безпосередньо: **https://resend.com/api-keys**

2. Натисніть **"Create API Key"** (Створити API ключ)

3. Заповніть форму:
   - **Name** (Назва): наприклад, `Astroline Production`
   - **Permission** (Дозвіл): виберіть **"Full Access"** (Повний доступ)
   - **Expires** (Термін дії): можна залишити "Never" (Ніколи) або встановити дату

4. Натисніть **"Add"** (Додати)

5. ⚠️ **ВАЖЛИВО:** Скопіюйте ключ зараз! Він показується тільки один раз.
   - Ключ починається з `re_`
   - Виглядає приблизно так: `re_1234567890abcdefghijklmnopqrstuvwxyz`

6. Збережіть ключ в безпечному місці (наприклад, в менеджері паролів)

---

## 📝 Крок 2: Додати ключі в Vercel

### Додати RESEND_API_KEY:

1. В Vercel Dashboard → ваш проєкт → **Settings** → **Environment Variables**
2. Натисніть **"Add Environment Variable"**
3. Заповніть:
   - **Name:** `RESEND_API_KEY`
   - **Value:** ваш ключ (той що починається з `re_`)
   - **Environments:** виберіть всі три:
     - ☑️ Production
     - ☑️ Preview
     - ☑️ Development
4. Натисніть **"Save"**

### Додати EMAIL_FROM:

1. Натисніть **"Add Environment Variable"** ще раз
2. Заповніть:
   - **Name:** `EMAIL_FROM`
   - **Value:** `Astroline <onboarding@resend.dev>` (для тестування)
   - **Environments:** виберіть всі три
3. Натисніть **"Save"**

**Примітка про EMAIL_FROM:**
- `onboarding@resend.dev` - це тестовий домен Resend, працює без верифікації
- Для продакшн потрібно верифікувати свій домен (інструкція нижче)

---

## 🔄 Крок 3: Redeploy проєкту

### Який деплой обрати?

**Виберіть найновіший Production деплой з маркером "Current":**

1. В Vercel Dashboard → **Deployments**
2. Знайдіть деплой з:
   - ✅ Маркером **"Current"** (поточний)
   - ✅ Environment: **Production**
   - ✅ Status: **Ready** (зелена точка)
   - ✅ Найновіший за датою

3. Натисніть на **три крапки (⋯)** справа від деплою

4. Виберіть **"Redeploy"**

5. Підтвердіть redeploy

**Альтернатива:** Просто зробіть новий коміт і push в GitHub - Vercel автоматично зробить новий деплой з новими змінними.

---

## 🎨 Як працює email в проєкті

### Процес відправки:

```
1. Користувач проходить квіз
   ↓
2. Генерується звіт (AI або статичний)
   ↓
3. Система викликає /api/send-report
   ↓
4. Resend отримує дані:
   - Email користувача
   - Report ID
   - Знаки зодіаку (Сонце, Місяць, Асцендент)
   ↓
5. Resend надсилає красивий HTML email
   ↓
6. Користувач отримує email з:
   - Привітанням
   - Натальною картою
   - Посиланням на повний звіт
```

### Що містить email:

- ✨ Красивий дизайн з градієнтами
- 🌌 Натальна карта (Сонце, Місяць, Асцендент)
- 📊 Короткий опис що в звіті
- 🔗 Кнопка "Переглянути повний звіт"
- 📧 Посилання на конфіденційність та відписку

### Де знаходиться код:

- **Email шаблон:** `src/emails/ReportEmail.tsx`
- **API endpoint:** `src/app/api/send-report/route.ts`
- **Resend конфігурація:** `src/lib/resend.ts`

---

## 🧪 Тестування email

### Після додавання ключів:

1. **Зробіть redeploy** (як описано вище)

2. **Протестуйте на сайті:**
   - Відкрийте ваш Vercel URL
   - Перейдіть на `/quiz`
   - Пройдіть квіз до кінця
   - Введіть свій email
   - Перевірте пошту (також перевірте спам!)

3. **Перевірте логи:**
   - Vercel Dashboard → **Functions**
   - Знайдіть `/api/send-report`
   - Перегляньте логи на помилки

### Очікуваний результат:

✅ Email приходить протягом 1-2 хвилин  
✅ Email має красивий дизайн  
✅ Посилання на звіт працює  
✅ В логах: `✅ Email sent successfully`

---

## 🏢 Верифікація домену (для продакшн)

### Якщо хочете використовувати свій домен:

#### 1. Додайте домен в Resend:

1. Перейдіть на **https://resend.com/domains**
2. Натисніть **"Add Domain"**
3. Введіть ваш домен (наприклад: `astroline.com`)
4. Натисніть **"Add"**

#### 2. Налаштуйте DNS записи:

Resend надасть вам DNS записи, які потрібно додати:

1. Перейдіть в панель управління вашим доменом (де ви купили домен)
2. Знайдіть розділ DNS налаштувань
3. Додайте записи, які надав Resend:
   - TXT записи для верифікації
   - MX записи (якщо потрібно)
   - SPF записи (якщо потрібно)

#### 3. Очікуйте верифікації:

- Resend перевірить домен (зазвичай 5-60 хвилин)
- Ви отримаєте email коли домен верифіковано

#### 4. Оновіть EMAIL_FROM:

1. В Vercel Dashboard → Environment Variables
2. Знайдіть `EMAIL_FROM`
3. Натисніть на три крапки → **"Edit"**
4. Змініть значення на: `Astroline <noreply@yourdomain.com>`
5. **Save**
6. Зробіть redeploy

---

## 💰 Ціни Resend

### Free Tier (Безкоштовний):
- ✅ 100 email/день
- ✅ 3,000 email/місяць
- ✅ Тестовий домен `onboarding@resend.dev`
- ✅ API доступ

### Pro Tier ($20/місяць):
- ✅ 50,000 email/місяць
- ✅ Кастомний домен
- ✅ Пріоритетна підтримка
- ✅ Детальна аналітика

**Для початку Free Tier цілком достатньо!**

---

## 🆘 Усунення проблем

### Email не приходить:

1. **Перевірте спам папку**
2. **Перевірте логи в Vercel:**
   - Functions → `/api/send-report` → View logs
   - Шукайте помилки
3. **Перевірте ключі:**
   - Чи правильно додано `RESEND_API_KEY`?
   - Чи правильно формат `EMAIL_FROM`? (має бути `Name <email@domain.com>`)
4. **Перевірте email адресу:**
   - Чи правильний формат email користувача?

### Помилка "Invalid API key":

1. Перевірте що ключ скопійовано повністю
2. Перевірте що немає пробілів на початку/в кінці
3. Перевірте що ключ починається з `re_`
4. Створіть новий ключ в Resend, якщо потрібно

### Email в спамі:

1. Верифікуйте свій домен (як описано вище)
2. Додайте SPF та DKIM записи
3. Використовуйте кастомний домен замість `onboarding@resend.dev`

---

## ✅ Чеклист

- [ ] Зареєстровано акаунт на Resend
- [ ] Створено API ключ
- [ ] Скопійовано ключ (починається з `re_`)
- [ ] Додано `RESEND_API_KEY` в Vercel (всі середовища)
- [ ] Додано `EMAIL_FROM` в Vercel (всі середовища)
- [ ] Зроблено redeploy проєкту
- [ ] Протестовано відправку email
- [ ] Email приходить успішно

---

## 📚 Додаткові ресурси

- [Resend Documentation](https://resend.com/docs)
- [Resend API Keys](https://resend.com/api-keys)
- [Resend Domains](https://resend.com/domains)
- [React Email Components](https://react.email/docs/components)

---

**Готово! Після виконання всіх кроків email функція буде працювати! 🚀**
