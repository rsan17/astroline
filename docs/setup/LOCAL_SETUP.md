# 🚀 Локальний запуск проєкту Astroline

## Швидкий старт

### 1. Перевірка Node.js
Проєкт потребує Node.js версії >= 18.0.0
```powershell
node --version
```
✅ Ваша версія: v24.12.0 (відмінно!)

### 2. Встановлення залежностей (якщо потрібно)
Якщо `node_modules` не існує, виконайте:
```powershell
cd "C:\Users\rober\Desktop\astro\astroline"
npm install
```

### 3. Налаштування змінних середовища (опціонально)
Створіть файл `.env.local` в папці `astroline`:

```env
# Обов'язкові (для повної функціональності)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Рекомендовані (для відправки email)
RESEND_API_KEY=your_resend_api_key_here
EMAIL_FROM=Astroline <noreply@astroline.com>

# Опціональні (для AI-генерації звітів)
# Принаймні один ключ рекомендується для кращих звітів
GROQ_API_KEY=gsk_your_groq_api_key_here
GOOGLE_GENERATIVE_AI_API_KEY=your_gemini_api_key_here
```

**Примітка:** 
- Якщо не вказати AI ключі, проєкт працюватиме зі статичними шаблонами
- Якщо не вказати Resend ключі, email функція буде вимкнена
- Проєкт працюватиме навіть без цих змінних!

### 4. Запуск сервера розробки
```powershell
cd "C:\Users\rober\Desktop\astro\astroline"
npm run dev
```

### 5. Відкрити в браузері
Після запуску відкрийте:
👉 **http://localhost:3000**

## Доступні команди

```powershell
npm run dev          # Запуск сервера розробки (http://localhost:3000)
npm run build        # Збірка для продакшн
npm run start        # Запуск продакшн сервера (після build)
npm run lint         # Перевірка коду ESLint
npm run type-check   # Перевірка TypeScript типів
```

## Структура проєкту

- `src/app/` - Сторінки та API routes
- `src/components/` - React компоненти
- `src/lib/` - Утиліти та сервіси (AI, email, SEO)
- `src/types/` - TypeScript типи

## Як отримати API ключі (опціонально)

### Groq API (рекомендовано)
1. Зареєструйтесь на https://console.groq.com
2. Перейдіть на https://console.groq.com/keys
3. Створіть новий API ключ
4. Безкоштовний рівень: ~6000 запитів/день

### Google Gemini API
1. Перейдіть на https://aistudio.google.com/app/apikey
2. Увійдіть з Google акаунтом
3. Створіть новий API ключ
4. Безкоштовний рівень: ~1500 запитів/день

### Resend API (для email)
1. Зареєструйтесь на https://resend.com
2. Перейдіть на https://resend.com/api-keys
3. Створіть новий API ключ
4. Для тестування можна використати: `onboarding@resend.dev`

## Усунення проблем

**Помилка при запуску?**
- Перевірте версію Node.js: `node --version` (потрібно >= 18.0.0)
- Видаліть `node_modules` та `.next`, потім: `npm install`

**Порт 3000 зайнятий?**
- Next.js автоматично знайде вільний порт (3001, 3002, тощо)
- Або встановіть інший порт: `npm run dev -- -p 3001`

**API не працює?**
- Перевірте змінні середовища в `.env.local`
- Перезапустіть сервер після зміни `.env.local`

## 📚 Додаткові гайди

Для детальної інформації дивіться:
- **`API_KEYS_SETUP.md`** - детальний гайд по налаштуванню всіх API ключів
- **`PROJECT_OVERVIEW.md`** - повний огляд проєкту, архітектури та технологій
- **`env.example`** - приклад файлу змінних середовища

## Готово! 🎉

Проєкт має працювати на http://localhost:3000
