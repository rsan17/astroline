# 📚 Огляд проєкту Astroline

Детальний опис архітектури, технологій та структури проєкту.

---

## 🎯 Що це за проєкт?

**Astroline** - це персоналізований астрологічний сервіс, який:
- 🔮 Генерує детальні астрологічні звіти на основі натальної карти
- 🤖 Використовує AI (Groq/Gemini) для персоналізації звітів
- 📧 Відправляє звіти на email
- 🎯 Надає прогнози на 2026 рік по кварталах
- 💕 Аналізує сумісність знаків зодіаку
- 🖐️ Аналізує долоню (coming soon)

---

## 🛠️ Технологічний стек

### Frontend
- **Next.js 14** (App Router) - React фреймворк
- **TypeScript** - типізація коду
- **Tailwind CSS** - стилізація
- **Framer Motion** - анімації
- **Zustand** - управління станом (квіз)
- **React Hook Form + Zod** - форми та валідація

### Backend / API
- **Next.js API Routes** - серверна логіка
- **Vercel AI SDK** - інтеграція з AI провайдерами
- **Resend** - відправка email

### AI Providers
- **Groq (LLaMA 3.3 70B)** - основний провайдер
- **Google Gemini 1.5 Flash** - резервний провайдер
- **Static Templates** - fallback, якщо AI недоступний

### Інші бібліотеки
- **astronomia** - астрологічні розрахунки
- **date-fns** - робота з датами
- **lucide-react** - іконки
- **react-datepicker** - вибір дати

---

## 📁 Структура проєкту

```
astroline/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── api/                # API endpoints
│   │   │   ├── generate-report/  # Генерація звіту
│   │   │   └── send-report/       # Відправка email
│   │   ├── quiz/               # Сторінка квізу
│   │   ├── report/[id]/        # Перегляд звіту
│   │   ├── zodiac/[sign]/      # Сторінки знаків зодіаку
│   │   ├── horoscope/[sign]/[period]/  # Гороскопи
│   │   ├── compatibility/      # Сумісність знаків
│   │   └── page.tsx             # Головна сторінка
│   │
│   ├── components/             # React компоненти
│   │   ├── landing/            # Компоненти головної сторінки
│   │   │   ├── Hero.tsx
│   │   │   ├── Features.tsx
│   │   │   ├── Testimonials.tsx
│   │   │   └── FAQ.tsx
│   │   │
│   │   ├── quiz/               # Компоненти квізу
│   │   │   ├── QuizContainer.tsx
│   │   │   ├── QuizProgress.tsx
│   │   │   └── steps/          # Кроки квізу (14 кроків)
│   │   │       ├── GenderStep.tsx
│   │   │       ├── BirthDateStep.tsx
│   │   │       ├── EmailStep.tsx
│   │   │       └── ...
│   │   │
│   │   ├── report/             # Компоненти звіту
│   │   │   ├── ReportHero.tsx
│   │   │   ├── NatalChartSection.tsx
│   │   │   ├── PersonalitySection.tsx
│   │   │   ├── ForecastSection.tsx
│   │   │   ├── LoveSection.tsx
│   │   │   ├── CareerSection.tsx
│   │   │   └── ...
│   │   │
│   │   ├── ui/                 # UI компоненти
│   │   │   ├── Button.tsx
│   │   │   ├── Input.tsx
│   │   │   ├── Card.tsx
│   │   │   └── ...
│   │   │
│   │   └── seo/                 # SEO компоненти
│   │       ├── JsonLd.tsx
│   │       └── Breadcrumbs.tsx
│   │
│   ├── lib/                    # Утиліти та сервіси
│   │   ├── ai/                 # AI інтеграція
│   │   │   ├── astro-report.ts    # Генерація звітів
│   │   │   └── prompts.ts         # AI промпти
│   │   ├── resend.ts           # Email сервіс
│   │   ├── report-data.ts      # Статичні дані звітів
│   │   ├── seo.ts              # SEO утиліти
│   │   └── utils.ts            # Загальні утиліти
│   │
│   ├── hooks/                  # React хуки
│   │   └── useQuizStore.ts     # Zustand store для квізу
│   │
│   ├── types/                  # TypeScript типи
│   │   └── report.ts           # Типи для звітів
│   │
│   └── emails/                 # Email шаблони
│       └── ReportEmail.tsx     # React Email компонент
│
├── public/                     # Статичні файли
│   └── manifest.json
│
├── .env.local                  # Змінні середовища (не в Git)
├── next.config.mjs             # Конфігурація Next.js
├── tailwind.config.ts          # Конфігурація Tailwind
└── package.json                # Залежності
```

---

## 🔄 Як працює проєкт

### 1. Квіз (Quiz Flow)

**Користувач проходить 14 кроків:**

1. **Стать** - вибір статі
2. **Дата народження** - введення дати
3. **Час народження** - опціонально
4. **Місце народження** - автодоповнення міст
5. **Статус відносин** - single/dating/married/complicated
6. **Життєві цілі** - вибір цілей (love, career, health, etc.)
7. **Улюблений колір** - вибір кольору
8. **Стихія** - вибір стихії (fire, water, earth, air)
9. **Розрахунок натальної карти** - автоматично
10. **Завантаження долоні** - опціонально
11. **Аналіз долоні** - автоматично (якщо завантажено)
12. **Email** - введення email
13. **Paywall** - оплата (якщо потрібно)
14. **Результат** - перегляд звіту

**Дані зберігаються:**
- В `localStorage` через Zustand persist
- В стані `useQuizStore`

### 2. Генерація звіту

**API Endpoint:** `/api/generate-report`

**Процес:**
```
1. Отримання даних з квізу
   ↓
2. Валідація дати народження та знаку зодіаку
   ↓
3. Розрахунок натальної карти (astronomia)
   ↓
4. Генерація базового статичного звіту
   ↓
5. Спроба AI-генерації:
   ├─ Groq (primary) → персоналізований звіт
   ├─ Gemini (fallback) → персоналізований звіт
   └─ Static (fallback) → базовий шаблон
   ↓
6. Об'єднання AI контенту зі статичною структурою
   ↓
7. Повернення звіту з reportId
```

**AI промпт включає:**
- Дані користувача (стать, дата, знаки)
- Життєві цілі
- Статус відносин
- Улюблений колір
- Стихію

**AI генерує:**
- Опис Сонця, Місяця, Асцендента
- Прогноз на 2026 (4 квартали)
- Любовний розділ
- Кар'єрний розділ
- Приховані таланти
- Життєві уроки

### 3. Відправка email

**API Endpoint:** `/api/send-report`

**Процес:**
```
1. Отримання email та reportId
   ↓
2. Валідація email
   ↓
3. Формування URL звіту
   ↓
4. Генерація React Email компонента
   ↓
5. Відправка через Resend
   ↓
6. Повернення результату
```

### 4. Перегляд звіту

**URL:** `/report/[id]`

**Компоненти:**
- `ReportHero` - заголовок звіту
- `NatalChartSection` - натальна карта
- `PersonalitySection` - особистість
- `ForecastSection` - прогноз 2026
- `LoveSection` - любов
- `CareerSection` - кар'єра
- `LuckySection` - щасливі числа/дні
- `ShareSection` - поділитись

---

## 🎨 Дизайн та UX

### Стилізація
- **Tailwind CSS** - utility-first CSS
- **Кастомні кольори** - темна тема з градієнтами
- **Responsive** - адаптивний дизайн для мобільних

### Анімації
- **Framer Motion** - плавні переходи між кроками квізу
- **Loading states** - індикатори завантаження
- **Confetti** - ефект після завершення квізу

### UX особливості
- Прогрес-бар квізу
- Можливість повернутись назад
- Збереження прогресу в localStorage
- Валідація форм в реальному часі

---

## 🔐 Безпека та конфіденційність

### Захист даних
- ✅ Дані не зберігаються в базі (поки що)
- ✅ Звіти доступні тільки по унікальному ID
- ✅ Email валідація
- ✅ HTTPS в продакшн

### API ключі
- ✅ Зберігаються в `.env.local` (не в Git)
- ✅ Використовуються тільки на сервері
- ✅ Fallback механізми, якщо ключі недоступні

---

## 📊 SEO оптимізація

### Реалізовано
- ✅ JSON-LD структуровані дані
- ✅ Meta теги (title, description, OG)
- ✅ Sitemap (`/sitemap.ts`)
- ✅ Robots.txt (`/robots.ts`)
- ✅ Breadcrumbs
- ✅ Сторінки для кожного знаку зодіаку

### Структуровані дані
- Organization
- WebApplication
- FAQ
- BreadcrumbList

---

## 🚀 Деплой

### Vercel (рекомендовано)
1. Push в GitHub
2. Імпорт в Vercel
3. Встановлення змінних середовища
4. Автоматичний деплой

### Інші платформи
- Може працювати на будь-якому хостингу з підтримкою Next.js
- Потрібно налаштувати змінні середовища

---

## 🧪 Тестування

### Локальне тестування
```bash
npm run dev          # Запуск сервера розробки
npm run build        # Перевірка збірки
npm run lint         # Перевірка коду
npm run type-check   # Перевірка TypeScript
```

### Тестування функцій
- Генерація звіту: `/api/generate-report`
- Відправка email: `/api/send-report`
- Перегляд звіту: `/report/[id]`

---

## 📈 Майбутні покращення

### Планується
- [ ] База даних (Supabase) для збереження звітів
- [ ] Аутентифікація користувачів
- [ ] Підписки та оплата (Stripe)
- [ ] Аналіз долоні (computer vision)
- [ ] Мобільний додаток
- [ ] Push-нотифікації

---

## 🛠️ Рекомендації по роботі з проєктом

### 1. Структура файлів
- Компоненти в `src/components/` за категоріями
- Утиліти в `src/lib/`
- Типи в `src/types/`
- API routes в `src/app/api/`

### 2. Внесення змін

**Для UI компонентів:**
1. Знайдіть компонент в `src/components/`
2. Внесіть зміни
3. Перевірте в браузері (hot reload)

**Для API:**
1. Знайдіть route в `src/app/api/`
2. Внесіть зміни
3. Перезапустіть сервер
4. Протестуйте через Postman або curl

**Для AI промптів:**
1. Відредагуйте `src/lib/ai/prompts.ts`
2. Перезапустіть сервер
3. Протестуйте генерацію звіту

### 3. Додавання нових функцій

**Новий крок квізу:**
1. Створіть компонент в `src/components/quiz/steps/`
2. Додайте в `QuizContainer.tsx`
3. Оновіть `TOTAL_STEPS` в `useQuizStore.ts`
4. Додайте дані в `QuizData` інтерфейс

**Новий розділ звіту:**
1. Створіть компонент в `src/components/report/`
2. Додайте в `src/app/report/[id]/page.tsx`
3. Оновіть типи в `src/types/report.ts`

### 4. Debugging

**Консоль сервера:**
- Показує який AI провайдер використовується
- Показує помилки API
- Показує процес генерації звіту

**React DevTools:**
- Перевірка стану Zustand store
- Перевірка пропсів компонентів

**Network tab:**
- Перевірка API запитів
- Перевірка відповідей

---

## 📚 Корисні посилання

- [Next.js Documentation](https://nextjs.org/docs)
- [Vercel AI SDK](https://sdk.vercel.ai/docs)
- [Zustand](https://zustand-demo.pmnd.rs/)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [Framer Motion](https://www.framer.com/motion/)

---

## ❓ Питання?

Якщо виникли питання:
1. Перевірте консоль сервера
2. Перевірте документацію вище
3. Перевірте файли проєкту для прикладів

---

**Готово до роботи! 🚀**
