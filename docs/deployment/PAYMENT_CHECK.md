# 🔍 Перевірка оплати в Production

## Проблема: Оплата скіпається

Якщо оплата автоматично скіпається без реальної транзакції, це означає що встановлено тестовий режим.

---

## ✅ Швидка перевірка

### 1. Перевірте змінну `SKIP_PAYMENT` в Vercel

1. Перейдіть в **Vercel Dashboard** → ваш проєкт
2. Відкрийте **Settings** → **Environment Variables**
3. Знайдіть змінну `SKIP_PAYMENT`
4. **Якщо вона встановлена в `true`** → це причина проблеми!

### 2. Виправлення

**Варіант A: Видалити змінну (рекомендовано для production)**
- Знайдіть `SKIP_PAYMENT` в списку
- Натисніть на три крапки (⋯) → **Delete**
- Підтвердіть видалення

**Варіант B: Встановити в `false`**
- Знайдіть `SKIP_PAYMENT`
- Натисніть **Edit**
- Змініть значення на `false`
- **Environments:** виберіть всі три (Production, Preview, Development)
- **Save**

### 3. Redeploy

Після зміни змінних потрібен redeploy:

1. Перейдіть в **Deployments**
2. Знайдіть останній деплой
3. Натисніть на три крапки (⋯)
4. Виберіть **"Redeploy"**

Або зробіть новий коміт і push в GitHub.

---

## 🔍 Як працює оплата

### Нормальний режим (`SKIP_PAYMENT` не встановлено або `false`)

1. Користувач натискає "Отримати доступ" на PaywallStep
2. Створюється платіж через Monobank API
3. Користувач перенаправляється на сторінку оплати Monobank
4. Після успішної оплати → redirect на `/api/monobank/success`
5. Звіт розблоковується (`isPaid = true`)

### Тестовий режим (`SKIP_PAYMENT=true`)

1. Користувач натискає "Отримати доступ"
2. **Оплата автоматично скіпається**
3. Користувач одразу перенаправляється на success сторінку
4. Звіт розблоковується без реальної оплати

---

## 📋 Чеклист для production

- [ ] `SKIP_PAYMENT` не встановлено **АБО** встановлено в `false`
- [ ] `MONOBANK_TOKEN` встановлено (для реальних платежів)
- [ ] `NEXT_PUBLIC_BASE_URL` встановлено правильно
- [ ] Перевірено що оплата працює (зробити тестовий платіж)

---

## 🧪 Коли використовувати тестовий режим

**Тестовий режим (`SKIP_PAYMENT=true`) корисний для:**
- Локального тестування
- Preview deployments (для тестування без реальних платежів)
- Development environment

**НЕ використовуйте в Production!**

---

## 🔧 Налаштування Monobank

Для роботи оплати потрібно:

1. **MONOBANK_TOKEN** - токен з web.monobank.ua
   - Перейдіть: web.monobank.ua → Інтернет → Управління еквайрингом
   - Створіть або скопіюйте токен
   - Додайте в Vercel як `MONOBANK_TOKEN`

2. **NEXT_PUBLIC_BASE_URL** - URL вашого сайту
   - Production: `https://your-domain.com`
   - Vercel: `https://your-project.vercel.app`

---

## 🐛 Troubleshooting

### Оплата не працює?

1. Перевірте логи в Vercel:
   - **Deployments** → останній деплой → **Functions** → `create-payment` → **View logs**
   - Шукайте помилки типу "Monobank is not configured"

2. Перевірте змінні:
   - `MONOBANK_TOKEN` встановлено?
   - `SKIP_PAYMENT` не встановлено або `false`?

3. Перевірте Monobank токен:
   - Чи валідний токен?
   - Чи не закінчився термін дії?

### Користувач може перейти до звіту без оплати?

Це нормально! Є кнопка **"Переглянути безкоштовний попередній перегляд"** яка дозволяє:
- Перейти до звіту без оплати
- Побачити обмежений контент (тільки перші секції)
- Побачити blur overlay на закритих секціях
- Побачити sticky CTA для оплати внизу сторінки

Це частина UX - користувач може побачити що отримає перед оплатою.

---

## ✅ Після виправлення

1. Зробіть redeploy
2. Протестуйте оплату:
   - Пройдіть квіз
   - Дійдіть до PaywallStep
   - Натисніть "Отримати доступ"
   - Перевірте що відкривається сторінка Monobank
   - Зробіть тестовий платіж (якщо можливо)
   - Перевірте що звіт розблоковується

---

**Готово! Оплата повинна працювати правильно! 💳**
