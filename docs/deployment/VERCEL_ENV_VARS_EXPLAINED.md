# 🔄 Як працюють Environment Variables в Vercel

## ✅ Коротка відповідь

**НІ!** Після того як ви додали ключі в Vercel один раз, вони **завжди** працюють автоматично в усіх майбутніх деплоях. Вам **НЕ потрібно** їх додавати знову при кожному push.

---

## 🎯 Як це працює

### Environment Variables зберігаються на рівні ПРОЄКТУ, не деплою

```
Vercel Project (astroline)
├── Settings → Environment Variables ← ТУТ зберігаються ключі
│   ├── GROQ_API_KEY
│   ├── GOOGLE_GENERATIVE_AI_API_KEY
│   ├── RESEND_API_KEY
│   └── EMAIL_FROM
│
└── Deployments (всі деплої автоматично отримують ці змінні)
    ├── Deployment 1 (Production)
    ├── Deployment 2 (Preview)
    ├── Deployment 3 (Production)
    └── ... (всі майбутні деплої)
```

### Що відбувається:

1. **Ви додаєте ключі один раз** в Settings → Environment Variables
2. **Vercel зберігає їх** на рівні проєкту
3. **Всі майбутні деплої автоматично отримують** ці змінні
4. **Вам НЕ потрібно** додавати їх знову

---

## 📊 Сценарії використання

### Сценарій 1: Новий push в GitHub

**Що ви робите:**
```bash
git add .
git commit -m "feat: new feature"
git push
```

**Що відбувається:**
1. Vercel автоматично виявляє push
2. Створює новий деплой
3. **Автоматично додає всі Environment Variables** з Settings
4. Білдить проєкт з усіма ключами
5. Деплоїть

**Вам потрібно щось робити?** ❌ НІ! Все автоматично.

---

### Сценарій 2: Pull Request

**Що ви робите:**
- Створюєте новий PR в GitHub

**Що відбувається:**
1. Vercel автоматично створює Preview деплой
2. **Автоматично додає Environment Variables** (якщо вони додані для Preview середовища)
3. Білдить проєкт
4. Деплоїть

**Вам потрібно щось робити?** ❌ НІ! Все автоматично.

---

### Сценарій 3: Ручний redeploy

**Коли потрібен:**
- Тільки **один раз** після додавання нових Environment Variables до існуючого деплою

**Що ви робите:**
1. Додали нові змінні в Settings
2. Зробили redeploy поточного деплою (щоб застосувати змінні)

**Після цього:**
- Всі майбутні деплої автоматично матимуть ці змінні
- Redeploy більше не потрібен

---

## 🔄 Життєвий цикл Environment Variables

```
1. Додаєте ключі в Settings → Environment Variables
   ↓
2. Один раз: Redeploy існуючого деплою (щоб застосувати)
   ↓
3. ВСІ майбутні деплої автоматично отримують ключі
   ↓
4. Push → автоматичний деплой з ключами ✅
   ↓
5. PR → автоматичний preview деплой з ключами ✅
   ↓
6. Будь-який новий деплой → автоматично з ключами ✅
```

---

## ⚙️ Налаштування середовищ

### Коли ви додаєте змінну, ви обираєте середовища:

- ☑️ **Production** - для основного сайту
- ☑️ **Preview** - для preview деплоїв (PR)
- ☑️ **Development** - для локальної розробки через Vercel CLI

### Рекомендація:

**Виберіть ВСІ ТРИ** для кожної змінної, щоб вони працювали скрізь.

---

## 🎯 Відповіді на ваші питання

### ❓ "Якщо я змінюю деплой, потрібно обирати current?"

**Відповідь:** НІ!

- Environment Variables зберігаються в Settings проєкту
- Вони автоматично доступні в усіх деплоях
- Вам не потрібно нічого обирати при кожному деплої

### ❓ "Коли я пушу зміни, потрібно змінювати ключ?"

**Відповідь:** НІ!

- Після додавання ключів один раз, вони працюють завжди
- Кожен новий деплой автоматично отримує всі змінні
- Вам не потрібно їх додавати знову

### ❓ "Коли потрібен redeploy?"

**Відповідь:** Тільки один раз!

- Після додавання **нових** Environment Variables
- Щоб застосувати їх до існуючого деплою
- Після цього всі майбутні деплої автоматично матимуть змінні

---

## 📝 Практичний приклад

### День 1: Додаєте ключі

1. Додаєте `GROQ_API_KEY` в Settings → Environment Variables
2. Додаєте `RESEND_API_KEY` в Settings → Environment Variables
3. Зробіть redeploy поточного деплою (один раз!)

### День 2: Push нових змін

```bash
git push
```

**Що відбувається:**
- ✅ Vercel автоматично створює новий деплой
- ✅ Автоматично додає `GROQ_API_KEY` та `RESEND_API_KEY`
- ✅ Все працює без вашого втручання

### День 3: Ще один push

```bash
git push
```

**Що відбувається:**
- ✅ Знову автоматично, ключі вже там
- ✅ Нічого додавати не потрібно

### День 4: Pull Request

- Створюєте PR
- ✅ Vercel автоматично створює Preview деплой
- ✅ Автоматично додає ключі (якщо вони додані для Preview)
- ✅ Все працює

---

## 🔒 Безпека

### Де зберігаються ключі:

- ✅ В зашифрованому вигляді в Vercel
- ✅ На рівні проєкту (не в коді)
- ✅ Не потрапляють в Git
- ✅ Доступні тільки вам (власнику проєкту)

### Як оновити ключ:

1. В Settings → Environment Variables
2. Знайдіть змінну (наприклад, `GROQ_API_KEY`)
3. Натисніть на три крапки → **"Edit"**
4. Введіть нове значення
5. **Save**
6. Зробіть redeploy (один раз, щоб застосувати до поточного деплою)

**Після цього:** Всі майбутні деплої автоматично отримають новий ключ.

---

## ✅ Підсумок

| Дія | Потрібно додавати ключі? |
|-----|-------------------------|
| Перший раз додаєте ключі | ✅ Так (один раз) |
| Push змін в GitHub | ❌ Ні (автоматично) |
| Pull Request | ❌ Ні (автоматично) |
| Ручний redeploy | ❌ Ні (ключі вже там) |
| Новий деплой | ❌ Ні (автоматично) |

---

## 🎉 Висновок

**Після того як ви додали ключі один раз:**

1. ✅ Вони зберігаються в Settings проєкту
2. ✅ Всі майбутні деплої автоматично отримують їх
3. ✅ Вам не потрібно нічого робити при кожному push
4. ✅ Все працює автоматично

**Redeploy потрібен був тільки один раз** - щоб застосувати змінні до існуючого деплою. Після цього всі нові деплої автоматично матимуть доступ до всіх Environment Variables.

---

**Просто працюйте як завжди - push, commit, PR - все працює автоматично! 🚀**
