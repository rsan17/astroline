# 🔑 Налаштування API ключів для Astroline

Цей гайд допоможе вам налаштувати всі API ключі для повної функціональності проєкту.

> **💡 Для Vercel:** Якщо ваш проєкт вже підключений до Vercel, дивіться **[VERCEL_API_KEYS_GUIDE.md](./VERCEL_API_KEYS_GUIDE.md)** - там покрокова інструкція як додати ключі в Vercel Dashboard, щоб вони завжди працювали в продакшн!

## 📋 Швидкий старт

1. Створіть файл `.env.local` в папці `astroline`
2. Додайте всі ключі з цього гайду
3. Перезапустіть сервер розробки (`npm run dev`)

---

## 🎯 1. Groq API (Рекомендовано - Основний AI провайдер)

**Що це:** Швидкий AI для генерації астрологічних звітів  
**Модель:** LLaMA 3.3 70B  
**Безкоштовний ліміт:** ~6000 запитів/день

### Як отримати:

1. Перейдіть на https://console.groq.com
2. Зареєструйтесь або увійдіть
3. Перейдіть на https://console.groq.com/keys
4. Натисніть **"Create API Key"**
5. Скопіюйте ключ (починається з `gsk_`)

### Додати в `.env.local`:

```env
GROQ_API_KEY=gsk_your_actual_key_here
```

### Переваги:
- ✅ Найшвидший AI провайдер
- ✅ Високий безкоштовний ліміт
- ✅ Використовується як основний (primary) провайдер

---

## 💎 2. Google Gemini API (Резервний AI провайдер)

**Що це:** Резервний AI для генерації звітів (якщо Groq недоступний)  
**Модель:** Gemini 1.5 Flash  
**Безкоштовний ліміт:** ~1500 запитів/день

### Як отримати:

1. Перейдіть на https://aistudio.google.com/app/apikey
2. Увійдіть з Google акаунтом
3. Натисніть **"Create API Key"**
4. Виберіть проєкт або створіть новий
5. Скопіюйте ключ (починається з `AIzaSy`)

### Додати в `.env.local`:

```env
GOOGLE_GENERATIVE_AI_API_KEY=AIzaSy_your_actual_key_here
```

### Коли використовується:
- 🔄 Автоматично, якщо Groq API недоступний або досягнуто ліміт
- 🔄 Якщо Groq повертає помилку

---

## 📧 3. Resend API (Відправка email)

**Що це:** Сервіс для відправки email з астрологічними звітами  
**Безкоштовний ліміт:** 100 email/день (free tier)

### Як отримати:

1. Перейдіть на https://resend.com
2. Зареєструйтесь
3. Перейдіть на https://resend.com/api-keys
4. Натисніть **"Create API Key"**
5. Скопіюйте ключ (починається з `re_`)

### Додати в `.env.local`:

```env
RESEND_API_KEY=re_your_actual_key_here
EMAIL_FROM=Astroline <noreply@astroline.com>
```

### Важливо про EMAIL_FROM:

**Для тестування (без верифікації домену):**
```env
EMAIL_FROM=Astroline <onboarding@resend.dev>
```

**Для продакшн (після верифікації домену):**
1. Перейдіть на https://resend.com/domains
2. Додайте свій домен
3. Налаштуйте DNS записи (Resend надасть інструкції)
4. Після верифікації використовуйте:
```env
EMAIL_FROM=Astroline <noreply@yourdomain.com>
```

---

## 🌐 4. NEXT_PUBLIC_APP_URL (URL проєкту)

**Що це:** Базовий URL вашого проєкту (для посилань у email та SEO)

### Для локальної розробки:

```env
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

### Для продакшн (Vercel):

```env
NEXT_PUBLIC_APP_URL=https://your-project.vercel.app
```

### Для кастомного домену:

```env
NEXT_PUBLIC_APP_URL=https://astroline.com
```

---

## 📝 Повний приклад `.env.local`

Створіть файл `astroline/.env.local` з таким вмістом:

```env
# ============================================
# AI Providers (для генерації звітів)
# ============================================

# Groq - Основний AI провайдер (рекомендовано)
GROQ_API_KEY=gsk_your_groq_key_here

# Google Gemini - Резервний AI провайдер
GOOGLE_GENERATIVE_AI_API_KEY=AIzaSy_your_gemini_key_here

# ============================================
# Email (Resend)
# ============================================

# Resend API ключ
RESEND_API_KEY=re_your_resend_key_here

# Email відправника (для тестування використовуйте onboarding@resend.dev)
EMAIL_FROM=Astroline <onboarding@resend.dev>

# ============================================
# App Configuration
# ============================================

# Базовий URL проєкту
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

---

## ✅ Перевірка налаштування

### 1. Перевірка AI ключів

Після запуску сервера (`npm run dev`), перевірте консоль:
- ✅ Якщо бачите `🤖 AI Available: true` - AI ключі працюють
- ❌ Якщо бачите `🤖 AI Available: false` - перевірте ключі

### 2. Тест генерації звіту

1. Відкрийте http://localhost:3000/quiz
2. Пройдіть квіз
3. Після генерації звіту перевірте консоль сервера:
   - `✅ Successfully generated AI content with Groq` - Groq працює
   - `✅ Successfully generated AI content with Gemini` - Gemini працює
   - `📄 Using static report` - AI не працює (перевірте ключі)

### 3. Тест відправки email

1. Після генерації звіту перевірте консоль:
   - `✅ Email sent successfully` - email працює
   - `❌ Failed to send email` - перевірте Resend ключі

---

## 🔄 Як працює система fallback

```
Користувач генерує звіт
    ↓
1. Спробувати Groq (якщо GROQ_API_KEY встановлено)
    ↓ (якщо помилка або ліміт)
2. Спробувати Gemini (якщо GOOGLE_GENERATIVE_AI_API_KEY встановлено)
    ↓ (якщо помилка)
3. Використати статичні шаблони (завжди працює)
```

**Важливо:** Проєкт працює навіть без AI ключів! Будуть використовуватись статичні шаблони.

---

## 🛠️ Усунення проблем

### AI не працює

**Проблема:** Консоль показує `📄 Using static report`

**Рішення:**
1. Перевірте, що ключі додані в `.env.local` (не `.env`)
2. Перевірте правильність ключів (без пробілів, повні ключі)
3. Перезапустіть сервер після зміни `.env.local`
4. Перевірте консоль на помилки API

### Email не відправляється

**Проблема:** `❌ Failed to send email`

**Рішення:**
1. Перевірте `RESEND_API_KEY` в `.env.local`
2. Перевірте формат `EMAIL_FROM`: `Name <email@domain.com>`
3. Для тестування використовуйте `onboarding@resend.dev`
4. Перевірте консоль на деталі помилки

### Помилка "Invalid API key"

**Рішення:**
1. Переконайтесь, що скопіювали повний ключ
2. Перевірте, що немає пробілів на початку/в кінці
3. Перевірте, що використовуєте правильний формат:
   - Groq: `gsk_...`
   - Gemini: `AIzaSy...`
   - Resend: `re_...`

---

## 🔒 Безпека

⚠️ **ВАЖЛИВО:**
- ❌ НІКОЛИ не комітьте `.env.local` в Git (вже в `.gitignore`)
- ❌ НЕ діліться своїми API ключами
- ✅ Використовуйте різні ключі для розробки та продакшн
- ✅ Для Vercel додавайте ключі через Dashboard → Settings → Environment Variables

---

## 📚 Додаткові ресурси

- [Groq Documentation](https://console.groq.com/docs)
- [Google Gemini Documentation](https://ai.google.dev/docs)
- [Resend Documentation](https://resend.com/docs)
- [Next.js Environment Variables](https://nextjs.org/docs/app/building-your-application/configuring/environment-variables)

---

## ✨ Готово!

Після налаштування всіх ключів ваш проєкт буде:
- ✅ Генерувати персоналізовані AI звіти
- ✅ Відправляти email з посиланнями на звіти
- ✅ Працювати з резервними механізмами

Якщо виникли питання - перевірте консоль сервера для деталей помилок.
